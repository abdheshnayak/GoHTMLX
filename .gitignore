examples/showcase/dist
testdata/golden/out
testdata/golden/out_multi
bin
node_modules
*.fiber.gz
validate

# Local build artifact (go build -o gohtmlx .)
gohtmlx

# Cursor IDE (local rules/settings, not for main)
.cursor